import React from "react";

export const metadata = {
  title: "Data Komponen Belanja",
};

function UserLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

export default UserLayout;
